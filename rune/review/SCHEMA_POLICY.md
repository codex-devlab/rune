# Review JSON Schema Policy

Versions 1.x are additive-only:
- MAY add new fields, new enum values, new top-level keys.
- MUST NOT remove, rename, retype, or narrow any existing field.

A breaking change requires major version bump to 2.0.
